# Database Backup & Restore Guide

- Automated daily backup snapshots are created in `backups/`.
- Admin can manually create snapshots via the UI at `/backups` or by running `scripts/backup.bat`.
- To restore a snapshot, select the backup file in `/backups` or run `scripts/restore.bat`.
