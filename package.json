{
  "name": "aura-textiles-pos",
  "version": "1.0.0",
  "description": "Aura Textiles — POS Billing & Inventory Software Desktop Application",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "npx esbuild backend/src/server.js --bundle --platform=node --target=node20 --outfile=backend/dist/server.cjs --format=cjs --external:electron --define:import.meta.url=__AURA_META_URL__ --banner:js=\"const __AURA_META_URL__=require('url').pathToFileURL(__filename).href;\"",
    "start": "node backend/src/server.js",
    "electron:dev": "electron .",
    "dist": "npm run build:frontend && npm run build:backend && electron-builder --win portable",
    "dist:installer": "npm run build:frontend && npm run build:backend && electron-builder --win nsis"
  },
  "build": {
    "appId": "com.auratextiles.pos",
    "productName": "Aura Textiles POS",
    "directories": {
      "output": "release"
    },
    "files": [
      "backend/**/*",
      "!backend/.env",
      "!backend/dist/**/*.map",
      "frontend/dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asar": true,
    "asarUnpack": [
      "backend/**/*",
      "frontend/dist/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Aura Textiles POS"
    }
  },
  "devDependencies": {
    "electron": "^34.2.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.4",
    "morgan": "^1.12.0"
  }
}
